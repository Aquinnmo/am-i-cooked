# Cloudflare Tunnel token (Zero Trust -> Networks -> Tunnels -> Docker connector)
TUNNEL_TOKEN=

# Backend runtime secrets
MONGODB_URI=
GEMINI_API_KEY=

# Comma-separated browser origins allowed to call this API
ALLOWED_ORIGINS=https://am-i-cooked-zeta.vercel.app
